apiVersion: apps/v1
kind: Deployment
metadata:
  name: supernode-2
  namespace: flower
  labels:
    app.kubernetes.io/name: supernode
    app.kubernetes.io/instance: supernode-2
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: supernode
      app.kubernetes.io/instance: supernode-2
  template:
    metadata:
      labels:
        app.kubernetes.io/name: supernode
        app.kubernetes.io/instance: supernode-2
    spec:
      containers:
        - name: supernode
          image: flwr/supernode:1.25.0
          args:
            - --superlink=superlink.flower.svc.cluster.local:9092
            - --clientappio-api-address=0.0.0.0:9095
            - --isolation=process
            - --node-config=partition-id=1 num-partitions=2
            - --root-certificates=/app/certificates/ca.crt
          ports:
            - name: clientappio
              containerPort: 9095
              protocol: TCP
          volumeMounts:
            - name: tls-certs
              mountPath: /app/certificates
              readOnly: true
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "2Gi"
              cpu: "2000m"
          livenessProbe:
            tcpSocket:
              port: clientappio
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: clientappio
            initialDelaySeconds: 10
            periodSeconds: 5
      volumes:
        - name: tls-certs
          secret:
            secretName: flower-tls
